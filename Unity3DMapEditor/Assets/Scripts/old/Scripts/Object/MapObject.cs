using System.Collections;
using UnityEngine;
public class CObject_Map : CObject_Phy
{
	  public override void SetPosition(Vector3 pos)
      {
          //注册到地图 todo

          base.SetPosition(pos);
      }
}	
