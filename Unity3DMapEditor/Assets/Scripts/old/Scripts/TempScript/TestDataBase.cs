using UnityEngine;
using System.Collections;
using DBSystem;
using DBC;

public class TestDataBase : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
		/*
		DBStruct dbs = new DBStruct();
		CDataBaseSystem dbManager = new CDataBaseSystem();
        dbManager.OpenAllDataBase(dbs.s_dbToLoad);
        COMMON_DBC<_DBC_ITEM_EQUIP_BASE> aaa = dbManager.GetDataBase<_DBC_ITEM_EQUIP_BASE>(2);
        _DBC_ITEM_EQUIP_BASE bbb = aaa.Search_Index_EQU(10000000);
		for(int i = 0 ; i < bbb.nBaseAttr.GetLength(0); i++)
		{
			for(int j = 0 ; j < bbb.nBaseAttr.GetLength(1); j++)
			{
				LogManager.Log(bbb.nBaseAttr[i,j]);
			}
		}
		*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
